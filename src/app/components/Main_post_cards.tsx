'use client';
import posts from '@/api/posts';
import { useQuery } from '@tanstack/react-query';
import Post_card from './Card_post';

const Board_main_post_card = () => {
  // const { data } = useQuery({ queryKey: ['posts'], queryFn: posts_call });
  // console.log(data);
  
  return (
    <>
      {/* 데이터 맵핑 */}
    </>
  )
}

export default Board_main_post_card;